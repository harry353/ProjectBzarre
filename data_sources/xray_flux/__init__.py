"""
GOES X-ray flux data source package.

Provides structured modules for download/ingest/plot so discovery tools
like `regenerate_db.py` can import the data source as part of the
`data_sources` namespace package.
"""
